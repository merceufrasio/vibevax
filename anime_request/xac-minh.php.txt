full url:
https://animevietsub.site/xac-minh.php?ref=aHR0cHM6Ly9hbmltZXZpZXRzdWIuc2l0ZS8=
after verified
https://animevietsub.site/xac-minh.php?ref=aHR0cHM6Ly9hbmltZXZpZXRzdWIuc2l0ZS9waGltL2thbWktbm8tc2hpenVrdS1hNTk3Ni90YXAtMDEtMTEyODE0Lmh0bWw=#
<!DOCTYPE html>
<html lang="vi">
<head>
<meta charset="UTF-8">
<meta name="title" content="Xác minh khu vực - AnimeVietSub" />
<title>Xác minh khu vực | AnimeVietSub</title>
<meta name="robots" content="noindex" />
<meta name="viewport" content="width=device-width, initial-scale=1">
<link rel="preconnect" href="https://fonts.googleapis.com">
<link href="https://fonts.googleapis.com/css2?family=Quicksand:wght@400;500;600;700&display=swap" rel="stylesheet">
<script src="https://code.jquery.com/jquery-3.7.1.min.js" type="ffda8853ecc317888f8c7465-text/javascript"></script>
<style>
*, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

body {
    font-family: 'Quicksand', sans-serif;
    background: #0d0d1a;
    min-height: 100vh;
    overflow-x: hidden;
    position: relative;
}
body::before {
    content: '';
    position: fixed; inset: 0;
    background:
        radial-gradient(ellipse at 20% 50%, rgba(255,62,108,0.12) 0%, transparent 60%),
        radial-gradient(ellipse at 80% 20%, rgba(99,102,241,0.10) 0%, transparent 50%),
        radial-gradient(ellipse at 50% 90%, rgba(236,72,153,0.08) 0%, transparent 50%);
    pointer-events: none; z-index: 0;
}

.wrapper {
    position: relative; z-index: 1;
    display: flex;
    min-height: 100vh;
    align-items: center;
    justify-content: center;
    gap: 40px;
    padding: 30px;
    max-width: 1100px;
    margin: 0 auto;
}

/* Left — map image */
.map-side {
    flex: 0 0 380px;
    text-align: center;
}
.map-side img {
    width: 100%;
    max-width: 340px;
    filter: drop-shadow(0 0 30px rgba(255,62,108,0.25));
    animation: floatMap 4s ease-in-out infinite;
}
@keyframes floatMap {
    0%,100% { transform: translateY(0); }
    50%     { transform: translateY(-10px); }
}
.map-title {
    margin-top: 18px;
    font-size: 15px;
    font-weight: 600;
    color: rgba(255,255,255,0.5);
    letter-spacing: 1px;
}
.map-notice {
    margin-top: 20px;
    background: rgba(220,38,38,0.18);
    border: 1.5px solid rgba(239,68,68,0.55);
    border-radius: 14px;
    padding: 18px 20px;
    text-align: left;
    box-shadow: 0 0 24px rgba(239,68,68,0.15), inset 0 0 0 1px rgba(255,255,255,0.04);
    animation: pulseBorder 2.5s ease-in-out infinite;
}
@keyframes pulseBorder {
    0%,100% { box-shadow: 0 0 24px rgba(239,68,68,0.15), inset 0 0 0 1px rgba(255,255,255,0.04); }
    50%      { box-shadow: 0 0 36px rgba(239,68,68,0.35), inset 0 0 0 1px rgba(255,255,255,0.04); }
}
.map-notice-title {
    display: flex;
    align-items: center;
    gap: 8px;
    font-size: 15px;
    font-weight: 700;
    color: #fca5a5;
    margin-bottom: 8px;
}
.map-notice-title::before {
    content: '⚠';
    font-size: 16px;
}
.map-notice-sub {
    font-size: 12.5px;
    color: rgba(255,255,255,0.6);
    line-height: 1.7;
}
.map-notice-sub strong { color: #fca5a5; }
.map-notice-caution {
    margin-top: 10px;
    padding-top: 10px;
    border-top: 1px solid rgba(239,68,68,0.3);
    font-size: 12px;
    color: #fca5a5;
    font-weight: 600;
    line-height: 1.6;
}

/* Right — form card */
.card {
    background: rgba(255,255,255,0.04);
    backdrop-filter: blur(24px);
    -webkit-backdrop-filter: blur(24px);
    border: 1px solid rgba(255,255,255,0.08);
    border-radius: 24px;
    width: 100%;
    max-width: 500px;
    padding: 36px 32px;
    box-shadow:
        0 0 0 1px rgba(255,62,108,0.06),
        0 30px 60px rgba(0,0,0,0.4);
}

.card-header {
    text-align: center;
    margin-bottom: 24px;
}
.card-header .logo {
    font-size: 28px;
    font-weight: 700;
    background: linear-gradient(135deg, #ff3e6c, #6366f1);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    margin-bottom: 4px;
}
.card-header h1 {
    font-size: 20px;
    font-weight: 700;
    color: #fff;
    margin-bottom: 6px;
}
.card-header p {
    font-size: 12.5px;
    color: rgba(255,255,255,0.45);
    line-height: 1.5;
}

.notice {
    background: rgba(220,53,69,0.15);
    border: 1px solid rgba(220,53,69,0.35);
    border-radius: 10px;
    padding: 10px 14px;
    font-size: 12.5px;
    color: #ff8a95;
    margin-bottom: 22px;
    text-align: center;
}

.form-group { margin-bottom: 16px; }
.form-group label {
    display: block;
    font-size: 13px;
    font-weight: 500;
    color: rgba(255,255,255,0.75);
    margin-bottom: 6px;
}
.form-group input {
    width: 100%;
    background: rgba(255,255,255,0.07);
    border: 1px solid rgba(255,255,255,0.15);
    border-radius: 10px;
    padding: 10px 14px;
    font-size: 14px;
    color: #fff;
    outline: none;
    transition: border-color 0.2s, background 0.2s;
}
.form-group input::placeholder { color: rgba(255,255,255,0.3); }
.form-group input:focus {
    border-color: #ff3e6c;
    background: rgba(255,62,108,0.08);
}
.form-group input.is-error { border-color: #ff5c6c; background: rgba(255,92,108,0.08); }
.field-error {
    display: flex;
    align-items: center;
    gap: 5px;
    margin-top: 5px;
    font-size: 12px;
    color: #ff8a95;
    animation: fadeIn 0.2s ease;
}
.field-error::before { content: '✕'; font-size: 11px; }
@keyframes fadeIn { from { opacity: 0; transform: translateY(-4px); } to { opacity: 1; transform: translateY(0); } }

.btn-submit {
    width: 100%;
    padding: 13px;
    background: linear-gradient(135deg, #ff3e6c, #6366f1);
    border: none;
    border-radius: 12px;
    color: #fff;
    font-size: 15px;
    font-weight: 700;
    font-family: 'Quicksand', sans-serif;
    cursor: pointer;
    margin-top: 6px;
    transition: opacity 0.2s, transform 0.15s, box-shadow 0.2s;
    letter-spacing: 0.5px;
    box-shadow: 0 4px 20px rgba(255,62,108,0.3);
}
.btn-submit:hover { opacity: 0.92; box-shadow: 0 6px 28px rgba(255,62,108,0.45); }
.btn-submit:active { transform: scale(0.97); }
.btn-submit:disabled { opacity: 0.5; cursor: not-allowed; box-shadow: none; }

.attempts-badge {
    display: inline-flex;
    align-items: center;
    gap: 6px;
    background: rgba(255,193,7,0.12);
    border: 1px solid rgba(255,193,7,0.3);
    border-radius: 8px;
    padding: 7px 12px;
    font-size: 12.5px;
    color: #ffd54f;
    margin-bottom: 10px;
    width: 100%;
}

.ip-badge {
    margin-top: 20px;
    text-align: center;
    font-size: 11.5px;
    color: rgba(255,255,255,0.25);
}

/* Toast */
#toast-container {
    position: fixed;
    top: 24px;
    right: 24px;
    z-index: 9999;
    display: flex;
    flex-direction: column;
    gap: 10px;
    pointer-events: none;
}
.toast-item {
    display: flex;
    align-items: flex-start;
    gap: 12px;
    min-width: 300px;
    max-width: 380px;
    padding: 14px 16px;
    border-radius: 14px;
    font-size: 14px;
    font-weight: 500;
    color: #fff;
    box-shadow: 0 8px 32px rgba(0,0,0,0.35);
    opacity: 0;
    transform: translateX(30px) scale(0.97);
    transition: opacity 0.3s ease, transform 0.3s ease;
    pointer-events: auto;
}
.toast-item.show { opacity: 1; transform: translateX(0) scale(1); }
.toast-item.toast-success { background: linear-gradient(135deg, #1db954, #17a045); }
.toast-item.toast-error   { background: linear-gradient(135deg, #e53935, #c62828); }
.toast-item.toast-warning { background: linear-gradient(135deg, #f9a825, #e65100); }
.toast-icon { font-size: 20px; flex-shrink: 0; }
.toast-body { flex: 1; }
.toast-title { font-weight: 700; font-size: 14px; margin-bottom: 2px; }
.toast-text  { font-size: 12.5px; opacity: 0.88; }
.toast-progress {
    position: absolute;
    bottom: 0; left: 0;
    height: 3px;
    background: rgba(255,255,255,0.4);
    border-radius: 0 0 14px 14px;
    animation: toastProgress linear forwards;
}
.toast-item { position: relative; overflow: hidden; }
@keyframes toastProgress { from { width: 100%; } to { width: 0%; } }

/* Success overlay */
#success-overlay {
    display: none;
    position: fixed;
    inset: 0;
    background: rgba(10,8,30,0.85);
    backdrop-filter: blur(8px);
    z-index: 10000;
    align-items: center;
    justify-content: center;
    flex-direction: column;
    gap: 16px;
}
#success-overlay.show { display: flex; }
.success-icon {
    width: 80px; height: 80px;
    background: linear-gradient(135deg, #1db954, #17a045);
    border-radius: 50%;
    display: flex; align-items: center; justify-content: center;
    font-size: 38px;
    box-shadow: 0 0 40px rgba(29,185,84,0.5);
    animation: popIn 0.4s cubic-bezier(0.175,0.885,0.32,1.275);
}
@keyframes popIn {
    from { transform: scale(0); opacity: 0; }
    to   { transform: scale(1); opacity: 1; }
}
.success-title {
    font-size: 22px; font-weight: 700; color: #fff;
    animation: fadeUp 0.4s ease 0.15s both;
}
.success-sub {
    font-size: 14px; color: rgba(255,255,255,0.6);
    animation: fadeUp 0.4s ease 0.25s both;
}
.success-bar-wrap {
    width: 220px; height: 4px;
    background: rgba(255,255,255,0.15);
    border-radius: 4px; overflow: hidden;
    animation: fadeUp 0.4s ease 0.3s both;
}
.success-bar {
    height: 100%;
    background: linear-gradient(90deg, #1db954, #7c6ff7);
    border-radius: 4px;
    width: 0%;
    transition: width 2.8s linear;
}
@keyframes fadeUp {
    from { opacity: 0; transform: translateY(12px); }
    to   { opacity: 1; transform: translateY(0); }
}

@media (max-width: 860px) {
    .wrapper { flex-direction: column; gap: 24px; padding: 20px; }
    .map-side { flex: none; }
    .map-side img { max-width: 200px; }
}
@media (max-width: 540px) {
    .card { padding: 28px 20px; }
    .card-header h1 { font-size: 18px; }
    #toast-container { top: 12px; right: 12px; left: 12px; }
    .toast-item { min-width: unset; max-width: 100%; }
}
</style>
</head>
<body>

<div id="toast-container"></div>

<div id="success-overlay">
    <div class="success-icon">✓</div>
    <div class="success-title">Xác minh thành công!</div>
    <div class="success-sub">Đang chuyển hướng, vui lòng chờ...</div>
    <div class="success-bar-wrap"><div class="success-bar" id="success-bar"></div></div>
</div>

<div class="wrapper">
    <div class="map-side">
        <img src="https://lh4.googleusercontent.com/R7nATBRPx4tzHiaauvNXccammL9Yz7w0LYnOdm_Ydn82CiuyXhAvECpa34Bh1MMUaZ07dTLeiMKiDQ1GrzUoxsiCJ3-g6WHWCmFfUMFM_RsyCJye0g9wX0CCHqNr=s0" alt="Bản đồ Việt Nam">
        <div class="map-title">🇻🇳 Hoàng Sa và Trường Sa là của Việt Nam.</div>
        <div class="map-notice">
            <div class="map-notice-title">Content has been removed!</div>
            <div class="map-notice-sub">Our service is <strong>not available</strong> in your area!<br>Dịch vụ của chúng tôi không hoạt động tại khu vực của bạn!</div>
            <div class="map-notice-caution">⛔ Caution! If you are not Vietnamese, please exit this page, it may be dangerous to you.</div>
        </div>
    </div>

    <div class="card">
        <div class="card-header">
            <div class="logo">AnimeVietSub</div>
            <h1>Xác minh người dùng Việt Nam</h1>
            <p>Nếu bạn là người Việt thì hãy điền thông tin phía bên dưới để xác minh:</p>
        </div>

        <form id="verify-form" action="#" method="post">
            <div class="form-group">
                <label>Ngày nhà giáo Việt Nam là ngày nào?</label>
                <input type="text" name="ngay_ng" data-field="ngay_ng" placeholder="dd/mm hoặc dd-mm" autocomplete="off">
            </div>
            <div class="form-group">
                <label>Đơn vị tiền tệ của Việt Nam là gì?</label>
                <input type="text" name="tiente" data-field="tiente" placeholder="Viết tắt hoặc tên đầy đủ" autocomplete="off">
            </div>
            <div class="form-group">
                <label>Quốc kỳ Việt Nam có ngôi sao mấy cánh?</label>
                <input type="text" name="quocky" data-field="quocky" placeholder="Số hoặc chữ tiếng Việt" autocomplete="off">
            </div>
            <div class="form-group">
                <label>Quần đảo Trường Sa, Hoàng Sa là của nước nào?</label>
                <input type="text" name="quandao" data-field="quandao" placeholder="Tên quốc gia" autocomplete="off">
            </div>
            <div class="form-group">
                <label>Điền từ còn thiếu: "...Việt Nam đẹp nhất có tên ___"</label>
                <input type="text" name="cautho" data-field="cautho" placeholder="Chỉ điền từ còn thiếu" autocomplete="off">
            </div>
            <button type="submit" class="btn-submit" id="btn-submit">Xác minh ngay →</button>
        </form>

        <div class="ip-badge">
            🌐 49.177.209.8                        — AU                    </div>
    </div>
</div>

<script type="ffda8853ecc317888f8c7465-text/javascript">
function showToast(type, title, text, duration) {
    duration = duration || 4000;
    var icons = { success: '✓', error: '✕', warning: '⚠' };
    var $toast = $(
        '<div class="toast-item toast-' + type + '">' +
            '<div class="toast-icon">' + (icons[type] || '●') + '</div>' +
            '<div class="toast-body">' +
                '<div class="toast-title">' + title + '</div>' +
                (text ? '<div class="toast-text">' + text + '</div>' : '') +
            '</div>' +
            '<div class="toast-progress" style="animation-duration:' + duration + 'ms"></div>' +
        '</div>'
    );
    $('#toast-container').append($toast);
    setTimeout(function() { $toast.addClass('show'); }, 10);
    setTimeout(function() {
        $toast.removeClass('show');
        setTimeout(function() { $toast.remove(); }, 350);
    }, duration);
}

$('#verify-form').on('submit', function() {
    $('#btn-submit').prop('disabled', true).text('Đang xác minh...');
});

</script>
<script src="/cdn-cgi/scripts/7d0fa10a/cloudflare-static/rocket-loader.min.js" data-cf-settings="ffda8853ecc317888f8c7465-|49" defer></script><script defer src="https://static.cloudflareinsights.com/beacon.min.js/v833ccba57c9e4d2798f2e76cebdd09a11778172276447" integrity="sha512-57MDmcccJXYtNnH+ZiBwzC4jb2rvgVCEokYN+L/nLlmO8rfYT/gIpW2A569iJ/3b+0UEasghjuZH/ma3wIs/EQ==" data-cf-beacon='{"version":"2024.11.0","token":"bdd9dbb8f233408aa0677d44852dfe8b","server_timing":{"name":{"cfCacheStatus":true,"cfEdge":true,"cfExtPri":true,"cfL4":true,"cfOrigin":true,"cfSpeedBrain":true},"location_startswith":null}}' crossorigin="anonymous"></script>
</body>
</html>
